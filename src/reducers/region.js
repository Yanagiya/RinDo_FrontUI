import * as types from '../actions/type'

const initialState = {
  firstRegion: null,
  secoundRegion: null,
  thirdRegion: null,
};

export default( state = initialState, action) => {
  const{ type, payload } = action;

}
